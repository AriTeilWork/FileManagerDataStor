Create functionality to read and write contacts to xml/json/sqlite file.
как это должно быть

1) програма в нынешнем виде должна иметь UI, можно в виде простой формы
после любого изменения списка контактов она должна сохранятся в xml/json/sqlite файлах
2) должна быть возможность получять эти данные и работать с ними